{
  "identifier": "default",
  "description": "Default permissions for Ledgit",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "store:default",
    "shell:default"
  ]
}
